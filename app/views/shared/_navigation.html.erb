<div class="nav">
  <%= content_tag :b, (link_to t('.app_name'), tests_path), class: "project-name"%>
  <% if user_signed_in? %>
    <%= content_tag :i, t('.welcome', email: current_user.email), class: "user" %>
    <%= link_to t('.log_out'), destroy_user_session_path, method: :delete %>
  <% else %>
    <%= link_to t('.sign_up'), new_user_registration_path %> | 
    <%= link_to t('.log_in'), new_user_session_path %>
  <% end %>
</div>