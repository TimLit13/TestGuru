<h3>Question</h3>
<p><%= @question.body %></p>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Delete', question_path(@question), 
                      data: { turbo_method: :delete, 
                              turbo_confirm: "Are you sure to delete #{@question.body} question?" 
                      } %>